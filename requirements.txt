# Python dependencies for the Policy Lens GENERATION pipeline.
# (The live site is static and needs none of this — these are only for regenerating the data.)
# Pinned to the versions used to produce the committed data.
anthropic==0.109.1
google-genai==2.8.0
